export interface Product {
    id: number;
    name: string;
    desc: string;
    price: number;
    un: string;
    img: string;
    code: string;
    paused: boolean;
    idCategory: number;
    createdAt: string;
    updatedAt: string;
}